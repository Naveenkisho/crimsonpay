export default function TrackingSettings({value,onChange}){const set=(key,next)=>onChange({...value,tracking:{...value.tracking,[key]:next}});return <section className="admin-card"><h2>Tracking</h2><p>Manage public Meta Pixel and Google tag IDs.</p><label className="admin-check"><input type="checkbox" checked={value.tracking.enabled} onChange={e=>set('enabled',e.target.checked)}/>Enable tracking</label><label className="admin-label">Meta Pixel ID</label><input className="admin-input" value={value.tracking.metaPixelId} onChange={e=>set('metaPixelId',e.target.value)}/><label className="admin-label">Google tag ID</label><input className="admin-input" value={value.tracking.googleTagId} onChange={e=>set('googleTagId',e.target.value)}/></section>}