export default function TrackingSettings({value,onChange}){const set=(key,next)=>onChange({...value,tracking:{...value.tracking,[key]:next}});return <section className="admin-card"><h2>Tracking</h2><p>Paste complete Meta Pixel and Google tag code snippets.</p><label className="admin-check"><input type="checkbox" checked={value.tracking.enabled} onChange={e=>set('enabled',e.target.checked)}/>Enable tracking</label><label className="admin-label">Meta Pixel code</label><textarea className="admin-input admin-code" rows="10" placeholder="<!-- Meta Pixel Code -->" value={value.tracking.metaPixelCode||''} onChange={e=>set('metaPixelCode',e.target.value)}/><label className="admin-label">Google tag code</label><textarea className="admin-input admin-code" rows="10" placeholder="<!-- Google tag code -->" value={value.tracking.googleTagCode||''} onChange={e=>set('googleTagCode',e.target.value)}/></section>}