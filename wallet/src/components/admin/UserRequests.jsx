import { useEffect,useState } from 'react';import { getCardRequests } from '../../lib/adminApi';export default function UserRequests(){const [rows,setRows]=useState([]),[message,setMessage]=useState('');useEffect(()=>{getCardRequests().then(setRows)},[]);async function copy(){await navigator.clipboard.writeText(rows.map(row=>`${row.countryCode} ${row.mobile}`).join('\n'));setMessage(`${rows.length} numbers copied`)}return <section className="admin-card admin-wide"><div className="admin-card-head"><div><h2>Card users</h2><p>Mobile numbers collected before secure wallet checkout.</p></div><button className="admin-btn" disabled={!rows.length} onClick={copy}>Copy all numbers</button></div>{message&&<div className="admin-copy-note">{message}</div>}<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Mobile</th><th>Country</th><th>Card</th><th>Colour</th><th>Requested</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><b>{row.countryCode} {row.mobile}</b></td><td>{row.country}</td><td className="capitalize">{row.card}</td><td className="capitalize">{row.theme}</td><td>{new Date(row.createdAt).toLocaleString()}</td></tr>)}</tbody></table>{!rows.length&&<p className="admin-empty">No card requests yet.</p>}</div></section>}