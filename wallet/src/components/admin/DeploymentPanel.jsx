const command='cd /opt/crimsonpay && git fetch origin main && git reset --hard origin/main && docker compose up -d --build --force-recreate';export default function DeploymentPanel({status}){return <section className="admin-card"><h2>Deployment & server</h2><p>Current standalone service status.</p><div className="admin-status">● {status?.online?'Online':'Checking…'}</div><label className="admin-label">Version</label><div>{status?.version||'—'}</div><label className="admin-label">Uptime</label><div>{status?Math.floor(status.uptimeSeconds/60)+' minutes':'—'}</div><div className="admin-actions"><button className="admin-btn secondary" onClick={()=>navigator.clipboard.writeText(command)}>Copy deployment command</button></div></section>}