import { useState } from 'react';import { adminLogin } from '../../lib/adminApi';export default function AdminLogin({onSuccess}){const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();setBusy(true);setError('');try{await adminLogin(password);onSuccess()}catch(cause){setError(cause.message)}finally{setBusy(false)}}return <main className="admin-page"><form className="admin-card admin-login" onSubmit={submit}><div className="admin-brand"><span className="admin-mark">◆</span><div><h1>CrimsonPay Admin</h1><p>Secure server controls</p></div></div><label className="admin-label">Admin password</label><input className="admin-input" type="password" value={password} onChange={e=>setPassword(e.target.value)} autoFocus required />{error&&<p className="admin-error">{error}</p>}<button className="admin-btn" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form></main>}